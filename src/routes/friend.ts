import { Request, Response } from 'express';

export const friend = {
    getFriendList : ( req:Request,res:Response )=>{
        return res.json("");
    },
    deleteFriend :( req:Request,res:Response )=>{
        return res.json("");
    },
}

